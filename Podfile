# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FirebaseFunction' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'
  pod 'Firebase/Messaging'
  pod 'EstimoteSDK'
  # Pods for FirebaseFunction

  target 'FirebaseFunctionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FirebaseFunctionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
